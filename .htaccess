<IfModule mod_rewrite.c>
  RewriteEngine on
  # if the file requested is an image, script, or stylesheet
  # ignore the request.
  RewriteRule \.(jpe?g|js|css|png|gif)$ - [L]
  # rewrite everything else to index.php/uri
  RewriteRule . index.php%{ENV:REQUEST_URI} [NE,L]
</IfModule>